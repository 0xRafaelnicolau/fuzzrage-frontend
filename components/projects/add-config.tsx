/*
                <CodeEditor
                    value={code}
                    language="yaml"
                    placeholder="Please paste your Echidna config here."
                    onChange={(evn) => setCode(evn.target.value)}
                    padding={15}
                    style={{
                        backgroundColor: "var(--background)",
                        fontFamily: "var(--font-mono)",
                        border: "solid var(--border)",
                        borderRadius: "var(--radius)"
                    }}
                />

Help me create a component that leverages the above code editor component to add a new config.

This component should allow for a config to also have a name associated with it.

*/